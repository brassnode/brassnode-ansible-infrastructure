---
all:
  hosts:
    localhost:
      ansible_connection: local
      ansible_python_interpreter: "{{ ansible_playbook_python }}"

  children:
    haraka:
      hosts:
        haraka-lint-placeholder:
          ansible_host: "127.0.0.1"
          ansible_connection: local
    relay:
      hosts: {}
